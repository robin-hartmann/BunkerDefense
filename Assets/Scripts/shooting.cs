using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using Valve.VR;

public class shooting : MonoBehaviour
{
    // Prints a joystick name if movement is detected.


    //public SteamVR_Actions_Single squeezeAction;

    /*void Update()
    {
        float triggerValue = squeezeAction.GetAxis(SteamVR_Inout_Sources.Any);

        if(triggerValue > 0.0f)
        {
            print(triggerValue);
        }
    }*/
}